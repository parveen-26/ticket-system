import { Request, Response } from "express";

export default class TicketController {

    /**
     * Create cinema with N tickets
     */
    public create = async (req: Request, res: Response) => {

    }
}